# Converge Studio

Professional browser-based BIM/model federation and coordination workspace with WebGPU rendering, model hierarchy/property inspection, selection sets, sections, measurements, clash/clearance processing, saved viewpoints, issue tracking, coordination reports, undo/redo, and project persistence.

The complete source is being committed to this repository. GitHub Pages publishes the standalone application from `docs/`.
